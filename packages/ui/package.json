{
  "name": "@quake-map/ui",
  "version": "1.0.0",
  "description": "Shared UI components for QuakeMap",
  "private": true,
  "svelte": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "svelte": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "svelte-check": "^3.6.0",
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "svelte": "^4.0.0"
  }
}
