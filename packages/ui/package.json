{
  "name": "@quake-map/ui",
  "version": "1.0.0",
  "description": "Shared UI components for QuakeMap",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'UI package built'",
    "dev": "echo 'UI package dev'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "svelte": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "svelte-check": "^3.8.6",
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "svelte": "^4.0.0"
  }
}
